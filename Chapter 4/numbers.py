#print numbers from one to twenty
numbers = list(range(1,21))
for number in numbers:
	print (number)

#print numbers from one to one million
numbers = list(range(1,1000001))
for number in numbers:
	print (number)

#use min max and sum functions on one million number list
print(min(numbers))
print (max(numbers))
print (sum(numbers))

